# kube-ui-server

```
$ kubebuilder init --domain k8s.appscode.com --skip-go-version-check
$ kubebuilder edit --multigroup=true

$ kubebuilder create api --group identity --version v1alpha1 --kind WhoAmI
```
